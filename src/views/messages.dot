{{##def.message:param:
	<div class="{{? param.incoming === true }}in{{??}}out{{?}} fancy-cf">
		<div class="time">{{= FancySupport.timeago(param.created) }}</div>
		<div class="message">{{! param.content }}</div>
	</div>
#}}


{{#def.message:it}}

{{~ it.replies :m }}
	{{#def.message:m}}
{{~}}
